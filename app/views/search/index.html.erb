<h1 class="text-centre"><%= pluralize(@products.count, "Result")%></h1>
<div class='container'>
  <table>
    <tbody>
      <% @products.each do |product| %>
        <tr>
          <td><%= link_to product.name, product %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
